.next
.yarn-integrity
coverage
node_modules
playwright-report
test-results
tests/e2e/screenshots
yarn-error.log
.DS_Store

# environment files (managed by Doppler — never commit secrets)
.env
.env.local
.env.docker
.env*.local

# build artifacts
*.tsbuildinfo

# local tooling state
.doppler
.claude/settings.local.json

# ZenStack intermediate Prisma schema
zenstack/~schema.prisma

# ralphex runtime (progress logs, worktrees)
.ralphex/progress/
.ralphex/worktrees/

# Claude Code worktrees
.claude/worktrees/
