.next
.yarn-integrity
coverage
node_modules
playwright-report
test-results
yarn-error.log
.DS_Store

# environment files (managed by Doppler — never commit secrets)
.env
.env.local
.env*.local

# local tooling state
.doppler
.claude/settings.local.json
